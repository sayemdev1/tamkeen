<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('newsletters', function (Blueprint $table) {
            $table->id();
            $table->string('title')->nullable(); // Title of the newsletter
            $table->text('content'); // Content of the newsletter
            $table->string('email')->unique(); // Email of the subscriber
            $table->string('name')->nullable(); // Name of the subscriber
            $table->boolean('is_active')->default(true); // Status of the newsletter
            $table->timestamps(); // Created at and updated at timestamps
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('newsletters');
    }
};
